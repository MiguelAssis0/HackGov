package com.fiap.hackgov.auth.internal.repositories;import com.fiap.hackgov.auth.internal.entities.UserSession;import org.springframework.data.jpa.repository.JpaRepository;import java.util.*;public interface UserSessionRepository extends JpaRepository<UserSession,UUID>{List<UserSession> findByUser_IdOrderByLastActivityDesc(UUID userId);Optional<UserSession> findByIdAndUser_Id(UUID id,UUID userId);}
